\documentclass[11pt,a4paper]{article}

% --- Packages ---
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[margin=0.6in]{geometry}
\usepackage{enumitem}
\usepackage{hyperref}
\usepackage{titlesec}
\usepackage{xcolor}
\usepackage{fontawesome5}
\usepackage{tabularx}
\usepackage[default]{sourcesanspro}

% --- Colors ---
\definecolor{primary}{HTML}{2E3440}
\definecolor{accent}{HTML}{4C566A}
\definecolor{link}{HTML}{5E81AC}

% --- Custom Formatting ---
\pagestyle{empty}
\setlist[itemize]{noitemsep, topsep=2pt, leftmargin=1.2em, labelsep=0.5em}
\hypersetup{colorlinks=true, linkcolor=link, urlcolor=link}

\titleformat{\section}{\large\bfseries\color{primary}\uppercase}{}{0em}{}[\titlerule]
\titlespacing*{\section}{0pt}{12pt}{8pt}

\begin{document}

% ── Header ────────────────────────────────────────────────────────────────────
\begin{center}
    {\Huge \textbf{\color{primary} (( profile.full_name ))}} \\ \vspace{6pt}
    \small
    ((* if profile.location *))
    \faMapMarker* \hspace{1pt} (( profile.location )) \quad
    ((* endif *))
    ((* if profile.phone *))
    \faPhone* \hspace{1pt} (( profile.phone )) \quad
    ((* endif *))
    ((* if profile.email *))
    \faEnvelope \hspace{1pt} \href{mailto:(( profile.email ))}{(( profile.email ))}
    ((* endif *))
    \\ \vspace{3pt}
    ((* if profile.linkedin_url *))
    \faLinkedin \hspace{1pt} \href{(( profile.linkedin_url ))}{(( profile.linkedin_url | replace("https://", "") ))} \quad
    ((* endif *))
    ((* if profile.github_url *))
    \faGithub \hspace{1pt} \href{(( profile.github_url ))}{(( profile.github_url | replace("https://", "") ))}
    ((* endif *))
    ((* if profile.website_url *))
    \quad \faGlobe \hspace{1pt} \href{(( profile.website_url ))}{(( profile.website_url | replace("https://", "") ))}
    ((* endif *))
\end{center}

% ── Summary ───────────────────────────────────────────────────────────────────
((* if profile.summary *))
\section*{Summary}
{\color{accent}
(( profile.summary ))}
((* endif *))

% ── Technical Skills ──────────────────────────────────────────────────────────
((* if skills *))
\section*{Technical Skills}
\begin{description}[font=\bfseries, style=multiline, leftmargin=6.5em]
((* for category, skill_list in skills.items() *))
    \item[(( category ))] (( skill_list | join(", ") ))
((* endfor *))
\end{description}
((* endif *))

% ── Work Experience ───────────────────────────────────────────────────────────
((* if experience *))
\section*{Work Experience}

((* for exp in experience *))
\noindent
\textbf{(( exp.company ))} \hfill \textbf{(( exp.start_date )) -- (( exp.end_date ))} \\
\textit{(( exp.role ))}((* if exp.location *)) \hfill (( exp.location ))((* endif *))
((* if exp.highlights *))
\begin{itemize}
((* for point in exp.highlights *))
    \item (( point ))
((* endfor *))
\end{itemize}
((* endif *))
((* if not loop.last *))
\vspace{6pt}
((* endif *))
((* endfor *))
((* endif *))

% ── Technical Projects ────────────────────────────────────────────────────────
((* if projects *))
\section*{Technical Projects}

((* for project in projects *))
\noindent
\textbf{(( project.title ))}((* if project.stack *)) \hfill \textit{(( project.stack | join(", ") ))}((* endif *))
\begin{itemize}
((* if project.bullets *))
(( project.bullets ))
((* elif project.description or project.metrics *))
((* if project.description *))
    \item (( project.description ))
((* endif *))
((* for m in project.metrics or [] *))
    \item (( m ))
((* endfor *))
((* endif *))
\end{itemize}
((* if not loop.last *))
\vspace{4pt}
((* endif *))
((* endfor *))
((* endif *))

% ── Education ─────────────────────────────────────────────────────────────────
((* if education *))
\section*{Education}
\begin{tabularx}{\textwidth}{Xr}
((* for edu in education *))
    \textbf{(( edu.institution ))} & \textbf{(( edu.graduation_year ))} \\
    ((* if edu.degree *))
    (( edu.degree ))((* if edu.field_of_study *)) --- (( edu.field_of_study ))((* endif *)) & ((* if edu.status *)) \textit{(( edu.status ))} ((* endif *)) \\
    ((* endif *))
    ((* if not loop.last *))\vspace{2pt} \\((* endif *))
((* endfor *))
\end{tabularx}
((* endif *))

\end{document}